[project]
name = "ogura"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["ogura-textdet"]

[project.optional-dependencies]
# Compatibility for existing detection training commands.
train = []
textrec = ["ogura-textrec"]

[tool.uv.sources]
ogura-textdet = { path = "textdet", editable = true }
ogura-textrec = { path = "textrec", editable = true }
