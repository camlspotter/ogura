[project]
name = "ogura"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pyarrow>=25.0.1",
]

[project.optional-dependencies]
train = [
    "fonttools>=4.55",
    "numpy>=2",
    "pillow>=11",
    "torch>=2.6",
]
